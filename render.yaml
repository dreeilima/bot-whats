services:
  - type: web
    name: bot-whats
    env: docker
    region: singapore # Região mais barata
    plan: free
    buildCommand: docker build -t bot-whats .
    startCommand: python setup.py
    envVars:
      - key: DATABASE_URL
        sync: false
        value: "postgres://postgres:Drey1992.@aws-0-us-west-1.pooler.supabase.com:6543/postgres?sslmode=require"
      - key: JWT_SECRET_KEY
        sync: false
      - key: WHATSAPP_NUMBER
        sync: false
    autoDeploy: true
    healthCheckPath: /health
